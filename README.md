# Fast Port Scanner (C++)

Um scanner de portas TCP ultra rápido desenvolvido em C++ utilizando **Multi-threading**.

## 🚀 Tecnologias
* C++ 11 ou superior.
* POSIX Sockets (Linux).
* Threads nativas do C++.

## 🛠️ Como Compilar
No seu terminal Linux, use o g++:
```bash
g++ -pthread core/scanner.cpp -o scanner
```
Como Usar
```Bash
./scanner
```
aviso: Use apenas para testes em seus próprios servidores.
